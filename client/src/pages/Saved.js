import React, { Component } from 'react'

export default class Saved extends Component {

    state = {

    };

    render() {
        return (
            <div>
                <h1>Saved Page</h1>
            </div>
        )
    }
}
